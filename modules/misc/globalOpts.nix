{
  globals.mapleader = " ";

  globalOpts = {
    number = true;
    relativenumber = true;

    # reserve some space on the left for some icons (breakpoints, warning, etc)
    signcolumn = "yes";

    cursorline = true;
  };
}
